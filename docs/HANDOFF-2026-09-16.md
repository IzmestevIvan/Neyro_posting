# Передача работы — 16 сентября 2026

## Проект и прод

Рабочий каталог: `/Users/hf/Desktop/Neyro_posting`. GitHub: `IzmestevIvan/Neyro_posting`, ветка `main`. Параметры SSH только в игнорируемом `.deploy.env`; не выводить ключи, токены и содержимое env. `deploy.sh` запускает pytest, доставляет код и пересобирает Docker Compose. Последняя успешная выкладка — около 15:43 UTC / 18:43 МСК. API-панель нагрузки уже работает на сервере.

## Каналы

Сейчас 14 каналов: 12 новостных с автопостингом (1,2,4,6–10,12–15), два бизнес-канала (5 DOBRA GROUP, 11 DIGIS). Официальный сайт DIGIS — digis.ru, подтверждён пользователем. Для бизнеса обязательное согласование; autopost=0, автоматическое создание черновиков включено. Профили не перезаписывать, компании не смешивать. Медиа бизнеса — проекты самой компании или загрузка пользователя, не стоки и не случайные картинки.

Новые 12–15 включены в 15:41:46 UTC: путешествия, автомобили, экономика, кибербезопасность. По четыре свежих источника; теперь всего 60 включённых новостных источников. Настройки новых: круглосуточно, instant/as_they_come, super, без дайджеста, hits_only=0/media_only=0, проверки фактов/темы/рекламы/дублей сохранены. Снимок предыдущих настроек в kv `load_trial:additional:2026-09-16`. Подробности и источники: `docs/api-load-2026-09-16.md`.

В последнем коротком срезе канал12 опубликовал один пост, 13/14/15 имели 1/4/2 материала new; готовой approved-очереди и failed/partial/uncertain у новых не было. Не считать это доказательством устойчивой работы всех каналов. Права can_post подтверждены у бота во всех 14 каналах.

## Изменения и проверки

- Админ: управление планами/лимитами клиентов и кодами доступа, ключами Gemini (включая формат AQ.), рассылки с подтверждением.
- API-нагрузка: реальные HTTP-попытки, 429/5xx/сеть/прерывания, успешные резервы, среднее время, активные/ожидающие HTTP-слоты, график 15 минут и суммы 60 минут. Буфер ограничен 61 минутной корзиной, без секретов и текстов, сброс при рестарте явно указан в UI. Это не процент квоты Google.
- Алерты: обычные сбои ИИ и успешные резервы остаются в журнале/админке; перед отправкой алерта о доставке повторно проверяется актуальное состояние поста. Описание фактической проблемы вместо общей фразы «требуется вмешательство». Подробности: `docs/alerts-2026-09-16.md`.
- Последний полный прогон: 341 pytest passed; `node tests/test_ui_state.cjs` passed; `tests/visual_smoke.cjs` без failures, просмотрен мобильный скрин админки. Read-only `scripts/release_smoke.py`: healthz, роли, 30 параллельных stats-запросов, права публикации — OK. Тестовые сообщения не отправлялись.

## Подтверждённые оставшиеся вопросы

1. Gemini часто возвращает 429 и иногда 503; при последнем старте все три проверки моделей получили 429. Это квота/доступность провайдера, не доказанная перегрузка CPU. Панель впервые показала 3 попытки/3 ошибки квоты; успешные резервы в предыдущем прогоне были. Не обещать, что квоты исправлены.
2. Разобрать задержки повторной AI-проверки (15 минут), маршруты/ключи и справедливость обслуживания каналов одного владельца. Не отключать фактчек ради нагрузки.
3. В двухчасовом прогоне пост1077 имел три неудачные HTTPStatusError-попытки без квитанций; точный HTTP-этап/причина ещё не установлены. Не перепубликовывать без проверки.
4. DIGIS не создавал черновик из-за поиска/выбора инфоповода; у DOBRA ранее был старый pending без изображения. Нужна отдельная проверка, это не исправлено добавлением метрик.

## Нагрузочные отчёты и ограничения

`docs/load-trial-2026-09-16.md` — первый этап. `docs/load-trial-expanded-2026-09-16.md` — строго 11:51:44–13:51:44 UTC, 44 публикации, 8 новостных + 2 бизнес, источники расширены в 11:59. Не смешивать поздние статусы с границей окна; пробел наблюдения описан. Автоматизация `neuropost` после итогов PAUSED, каналы не остановлены. Нового двухчасового расписания после добавления 12–15 не создавали. Это реальная нагрузка 14 каналов, не тест 120.

Не публиковать секреты или частные тексты при чтении логов; выводить агрегаты/обезличенные причины. Ничего не отправлять в Telegram как тест. Не останавливать каналы без конкретной угрозы дублей. Старые бизнес-профили, пользовательские настройки и тарифы сохранять.
